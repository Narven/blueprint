version: 1

name: Files using templates

force: true

source: .

output: ./examples/gen

structure:
  - Folder1:
      - extra|empty
      - index.tsx|reactdom
  - Folder1:
      - index.tsx|reactdom

templates:
  - empty
  - reactdom: >+
      ReactDOM.render(
        <h1>Hello, world!</h1>,
        document.getElementById('root')
      );
